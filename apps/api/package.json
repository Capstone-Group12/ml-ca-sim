{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "build": "echo \"No build step for FastAPI backend\"",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000"
  },
  "dependencies": {},
  "devDependencies": {}
}
