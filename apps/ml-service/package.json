{
  "name": "ml-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --host 0.0.0.0 --port 8001 --reload",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8001",
    "build": "echo \"No build step for ml-service\""
  },
  "dependencies": {},
  "devDependencies": {}
}
